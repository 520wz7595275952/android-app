<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.promptimagemanager">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PromptImageManager"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AddImageActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ImageDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.CategoryActivity"
            android:exported="false" />

        <!-- API相关Activity -->
        <activity
            android:name=".ui.ApiConfigActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ApiConfigEditActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ChatActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ImageGenerationActivity"
            android:exported="false" />

        <activity
            android:name=".ui.VideoGenerationActivity"
            android:exported="false" />

        <!-- Receive shared images from other apps -->
        <activity
            android:name=".ui.ShareReceiverActivity"
            android:exported="true"
            android:theme="@style/Theme.PromptImageManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- FileProvider for sharing images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
